# Backend

Express API used by the Vue Query demo.

## Responsibilities

- adds a fixed delay to every request to make async state visible
- exposes products and orders endpoints
- provides an intentionally unstable endpoint for retry demos

## Commands

```bash
pnpm dev
pnpm start
pnpm build
```

## Default Port

`http://localhost:3000`
